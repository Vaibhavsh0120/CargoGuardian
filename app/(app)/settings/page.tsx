import { RoutePlaceholder } from "@/components/layout/RoutePlaceholder";

export default function SettingsPage() {
  return (
    <RoutePlaceholder
      eyebrow="Settings"
      title="Settings page placeholder"
      description="Operator preferences, integration status, and system settings are added in later phases."
    />
  );
}
