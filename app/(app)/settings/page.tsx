import { RoutePlaceholder } from "@/components/layout/RoutePlaceholder";

export default function SettingsPage() {
  return (
    <RoutePlaceholder
      eyebrow="Settings"
      title="Settings"
      description="Operator preferences, profile controls, and integration settings will be managed here as those workflows come online."
    />
  );
}
